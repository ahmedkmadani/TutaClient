<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tuta.tutadriver">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TutaDriver">
        <activity android:name=".activity.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.WelcomeActivity"></activity>
        <activity android:name=".activity.LoginActivity"></activity>
        <activity android:name=".activity.SignupActivity"></activity>
        <activity android:name=".activity.TermsActivity"></activity>
        <activity android:name=".activity.CreateAccountActivity"></activity>
        <activity android:name=".activity.AddVehicleActivity"></activity>
        <activity android:name=".activity.AddDocumentActivity"></activity>
        <activity android:name=".activity.PaymentDetailsActivity"></activity>
        <activity android:name=".activity.DashboardActivity"></activity>
        <activity android:name=".activity.PinVerficationActivity"></activity>
    </application>

</manifest>